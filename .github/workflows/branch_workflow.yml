name: Branch CI
on:
  push:
    branches-ignore:
      - main
    workflow_dispatch:
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
  lint:
    uses: ./.github/workflows/lint.yml